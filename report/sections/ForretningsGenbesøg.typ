= Omskrivning af problemformulering
#{
  set heading(offset: 3)
  include "problemformulering2.typ"
}
= Reflektion over "fit" mellem vores produkt og problemformuleringen
#{
  set heading(offset: 3)
  include "er der fit.typ"
}
