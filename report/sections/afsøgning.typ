= Tidlig kontakt
#{
  set heading(offset: 3)
  include "tidlig-kontakt.typ"
}

= Første møde med ledelse
#{
  set heading(offset: 3)
  include "første-møde-med-ledelse.typ"
}

= Hvad er potentialet?
#{
  set heading(offset: 3)
  include "hvad-er-potentialet.typ"
}

= Interviewteknik (Et skridt tilbage)
#{
  set heading(offset: 3)
  include "interviewteknik.typ"
}
