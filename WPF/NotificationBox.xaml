<UserControl x:Class="WPF.NotificationPopup"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">

    <!-- Popup-kontrollen sørger for at ligge "øverst" uanset andet layout -->
    <Popup x:Name="ParentPopup" 
           IsOpen="{Binding IsNotificationOpen}" 
           AllowsTransparency="True" 
           StaysOpen="False" 
           Placement="Center" 
           PopupAnimation="Fade">

        <Border Background="#1E293B" CornerRadius="12" Padding="20,15" Margin="20">
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" Opacity="0.3" ShadowDepth="2"/>
            </Border.Effect>

            <StackPanel Orientation="Horizontal">
                <TextBlock Text="✔" Foreground="#10B981" FontWeight="Bold" Margin="0,0,12,0" VerticalAlignment="Center"/>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="Success!" FontWeight="Bold" Foreground="White" FontSize="14"/>
                    <TextBlock Text="{Binding NotificationMessage}" Foreground="#94A3B8" FontSize="13"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Popup>
</UserControl>